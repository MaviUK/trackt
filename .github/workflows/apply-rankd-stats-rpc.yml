name: Apply Rankd stats and focused ranking fixes

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Patch Rankd files
        run: |
          python3 <<'PY'
          from pathlib import Path
          import re
